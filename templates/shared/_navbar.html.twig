<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/">Symshop</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            {% for c in categoryRepository.findAll() %}
                <li class="nav-item">
                    <a href="{{ path('product_category', {'slug': c.slug}) }}" class="nav-link text-white">{{ c.name }}</a>
                </li>
            {% endfor %}
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item"><a href="{{ path('cart_show') }}"class="nav-link"><i class="fas fa-cart-plus text-white"> Mon Panier <strong>{{ cartService.total /100}} €</strong></i></a></li>
            {% if app.user %}
                <li class="nav-item"><a href="{{ path('purchase_index') }}" class="nav-link text-white">Mes commandes</a></li>
                <li class="nav-item"><a href="{{ path('security_logout') }}" class="btn btn-danger">Déconnexion</a></li>
            {% else %}
                <li class="nav-item"><a href="#" class="nav-link text-white">Inscription</a></li>
                <li class="nav-item"><a href="{{ path('security_login') }}" class="btn btn-success">Connexion</a></li>
            {% endif %}

        </ul>
    </div>
</nav>